import { Config } from '../config';
import { Stats } from '../types';
export default function createRouteFilter(config: Pick<Config, 'routes' | 'pathPrefix'>, stats?: Stats): (this: any, obj: {
    [name: string]: any;
}, routeName: string) => string;
