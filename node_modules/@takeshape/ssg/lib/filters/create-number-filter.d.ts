import { FormatLocaleDefinition } from 'd3-format';
import { Config } from '../config';
export default function createNumberFilter({ numbers, locale }: Pick<Config, 'numbers' | 'locale'>): (value: string | number, formatStr?: string | undefined, config?: Partial<FormatLocaleDefinition> | undefined) => string;
