import { Config, DatesConfig } from '../config';
export default function createDateFilter({ dates, locale: defaultLocale }: Pick<Config, 'dates' | 'locale'>): (raw: string, options?: string | Partial<DatesConfig>) => string;
