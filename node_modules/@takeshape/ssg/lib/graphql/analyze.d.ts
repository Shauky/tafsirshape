import { GraphQLSchema } from 'graphql';
import { DocumentNode } from './ast';
import { Config, FileLoader, RouteInfo } from '../config';
interface FileInfo {
    body: string;
    name: string;
}
interface Usage {
    file: string;
    location: {
        line: number;
        column: number;
    };
}
interface UsageInfo {
    type: string;
    usage: Usage[];
}
interface UsageInfoMap {
    [type: string]: {
        [name: string]: UsageInfo;
    };
}
export declare function getFieldUsage(queryAst: DocumentNode, schema: GraphQLSchema, fileInfo: FileInfo): UsageInfoMap;
export declare function detectTypes(queryAst: DocumentNode, schema: GraphQLSchema): Set<string>;
export interface TypeUsage {
    [type: string]: RouteInfo[];
}
export interface GetTypeUsageParams {
    config: Config;
    schema: GraphQLSchema;
    fileLoader: FileLoader;
}
export declare function getTypeUsage({ config, schema, fileLoader }: GetTypeUsageParams): Promise<TypeUsage>;
export {};
