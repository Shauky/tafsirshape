import { PathItemContext } from './types';
import { Data } from '../types';
import { Config, RouteConfig } from '../config';
export declare function combineContext(env: Config['env'], globalContext: Data, route: RouteConfig, routeContext: Data): Data;
export interface GetListPageContext {
    (items: Data[], pageNumber: number, routeContext: Data): Data;
}
export declare const getListPageContext: GetListPageContext;
export interface GetItemContext {
    (route: RouteConfig, context: Data, { current, previous, next }: PathItemContext): Data;
}
export declare const getItemContext: GetItemContext;
