import ExtendableError from 'es6-error';
import { TemplateLocation } from '../types';
export default class TemplateRenderError extends ExtendableError {
    constructor(message: string, location?: TemplateLocation);
}
