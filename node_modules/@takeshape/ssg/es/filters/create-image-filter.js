import { getImageUrl } from '@takeshape/routing';
export default function createImageFilter(baseUrl, imageDefaults = {}) {
  return (key, query) => {
    const imageParams = { ...imageDefaults,
      ...query
    };
    return getImageUrl(key, imageParams, {
      baseUrl
    });
  };
}